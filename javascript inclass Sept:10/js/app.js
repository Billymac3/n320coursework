var workerClockIn = {
  clockIn: function(employee) {
    // inform boss of clockin
    // add a timestamp
    // give employee daily tasks
  }
  clockOut: function() {
    // tell boss when employee clocked out
    // add timestamp
  }
};
